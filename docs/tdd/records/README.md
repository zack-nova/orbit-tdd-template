# TDD Records

Create one record per TDD task in this directory.

Use `docs/tdd/templates/tdd-record.md` as the starting shape. Runtime records are
work evidence, not template source; published template payloads should keep this
README and the template, but not copy completed task records by default.
